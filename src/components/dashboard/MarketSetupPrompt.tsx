// Re-export the MarketSelector as the default prompt
// This provides backward compatibility while using the new enhanced component
import MarketSelector from './MarketSelector';

export default MarketSelector;
